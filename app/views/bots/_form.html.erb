<%= form_for @bot do |f| %>

  <% if @bot.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@bot.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @bot.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </p>

  <p>
    <%= f.label :population %><br>
    <%= f.text_field :population %>
  </p>


  <p>
    <%= f.submit %>
  </p>

<% end %>
